.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.db
*.sqlite3
config.json
trading_settings.json
keys.bat
.env
*.log
dist/
build/
*.egg-info/
# ── Alien AI Trader — Sensitive Files ──────────────────────
# Add these to your .gitignore to prevent key exposure

# Local environment & keys
keys.bat
.env
.env.local
.env.production

# Runtime-generated settings (saved by dashboard on user changes)
trading_settings.json
install_config.json

# Per-machine license activation (issued after purchase — never commit)
license.json

# Live Alpaca keys entered in-app (real trading secrets — never commit)
live_keys.json

# Virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.pyc
*.pyo

# OS files
.DS_Store
Thumbs.db

# Project backups
backup files zipped/
*.bak

# Claude local worktrees
.claude/worktrees/
.claude/

# Local emergency / maintenance scripts
EMERGENCY_FIX_NOW_GITBASH.sh
RUN_EMERGENCY_FIX_GITBASH.cmd

# Local backup exports created during repair work
_pre_fix_backup_*/
_pre_render_store_backup_*/

# Generated installer branding assets
installer/alien_icon.ico
installer/alien_icon.png

# Standalone apps live outside this project (../Standalone Apps/)
black_box/
adam_agent/
agent ADAM/
user_cloud/

# Local snapshot zips + personal settings notes — keep out of the public repo
snapshots/
trader settings.txt
more settings.txt*
Recommended Render Environment Variables and Values.docx